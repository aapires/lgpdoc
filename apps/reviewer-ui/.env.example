# Copy to .env.local and tweak as needed.

# Where the FastAPI service is listening.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:9000

# Set to "true" to skip the network and use src/lib/mocks.ts data.
NEXT_PUBLIC_USE_MOCKS=false
